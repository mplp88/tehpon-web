{
  "name": "tehpon-web-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd web-api && npm install"
  }
}
